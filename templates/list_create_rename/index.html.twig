{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col text-center">
            <h1>{{ caption }}</h1>
        </div>
    </div>
    <div class="row justify-content-center">
        <div class="col-auto">
            {{ form_start(listCreateForm) }}
            {{ form_row(listCreateForm.name) }}
            <button type="submit" class="btn btn-primary">Создать</button>
            <a href="{{ path('app_lists') }}" class="btn btn-outline-secondary">Отмена</a>
            {{ form_end(listCreateForm) }}
        </div>
    </div>
{% endblock %}
